terraform {
  required_version = ">= 1.7.0"

  required_providers {
    azurerm = {
      source  = "hashicorp/azurerm"
      version = "~> 4.0"
    }
  }
}

provider "azurerm" {
  features {}

  # AzureRM v4 requires the subscription ID to be explicit.
  subscription_id = var.subscription_id
}
